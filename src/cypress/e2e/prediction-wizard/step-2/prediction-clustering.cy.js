import { selectClusteringModel } from "../helper";

describe("If the user chose a clustering model type, they skip the target variable selection screen", () => {
  it("If the user chose a clustering model type, they skip the target variable selection screen", () => {
    selectClusteringModel();
  });
});
